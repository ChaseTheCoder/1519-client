import React from 'react'
import LoginPanel from '../LoginPanel/LoginPanel';

export default function Login() {
        return (
            <div>
                <LoginPanel />
            </div>
        );
}